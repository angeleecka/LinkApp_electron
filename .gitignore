# ── Node
node_modules/
desktop/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Логи
*.log
logs/
*.pid
*.pid.lock

# ── Кэши/сборки
dist/
build/
out/
.cache/
.tmp/

# ── Electron builder output (инсталляторы и портейбл)
desktop/dist/
desktop/release/
desktop/build/
desktop/out/

# Артефакты сборки по расширениям (оставим в пределах desktop/*)
desktop/**/*.exe
desktop/**/*.dmg
desktop/**/*.AppImage
desktop/**/*.snap
desktop/**/*.deb
desktop/**/*.rpm
desktop/**/*.msi
desktop/**/*.zip
desktop/**/*.7z

# ── ОС-мусор
.DS_Store
Thumbs.db

# ── Локальные env-файлы (пример .env.example можно коммитить)
.env
.env.local
*.env.local
desktop/.env
desktop/.env.local

# ── Экспортированные данные приложения (если сохраняешь рядом)
LinkApp-data.json

# ── Временные файлы
*.tmp

# ── Иде/редакторы (по желанию; если настройки проекта нужны — оставь)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json
